{
  "name": "notebook-app-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Backend API service for the personal notebook app",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "migrate": "ts-node src/db/migrate.ts",
    "predev": "npm run migrate",
    "dev": "ts-node src/server.ts",
    "prestart": "node dist/db/migrate.js",
    "start": "node dist/server.js",
    "pretest": "cross-env NODE_ENV=test ts-node src/db/migrate.ts",
    "test": "jest --runInBand",
    "test:coverage": "jest --runInBand --coverage"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.4.0",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/express": "^4.17.21",
    "@types/jest": "^29.5.12",
    "@types/jsonwebtoken": "^9.0.6",
    "@types/multer": "^2.2.0",
    "@types/node": "^20.11.30",
    "@types/pg": "^8.23.1",
    "@types/supertest": "^6.0.2",
    "cross-env": "^10.1.0",
    "jest": "^29.7.0",
    "supertest": "^6.3.4",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
